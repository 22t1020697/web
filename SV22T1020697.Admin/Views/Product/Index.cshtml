@model ProductSearchInput
@{
    ViewBag.Title = "Quản lý Mặt hàng";
}

@section PageToolBar {
    <a href="~/Product/Create" class="btn btn-primary">
        <i class="bi bi-plus-circle me-1"></i>
        Bổ sung
    </a>
}

<div class="card mb-3">
    <div class="card-body">
        <form id="formSearch"
              method="get"
              action="~/Product/Search"
              data-target="searchResult"
              onsubmit="paginationSearch(event, this, 1)">

            <input type="hidden" asp-for="PageSize" />

            <div class="row g-2">
                <div class="col-md-3">
                    <label class="form-label">Loại hàng</label>
                    <select class="form-select" asp-for="CategoryID" asp-items="ViewBag.Categories">
                        @* Thẻ -- Tất cả -- thường đã được thêm trong SelectListHelper *@
                    </select>
                </div>

                <div class="col-md-3">
                    <label class="form-label">Nhà cung cấp</label>
                    <select class="form-select" asp-for="SupplierID" asp-items="ViewBag.Suppliers">
                        @* Thẻ -- Tất cả -- thường đã được thêm trong SelectListHelper *@
                    </select>
                </div>

                <div class="col-md-3">
                    <label class="form-label">Khoảng giá</label>
                    <div class="input-group">
                        <input asp-for="MinPrice" class="form-control" placeholder="Từ" />
                        <span class="input-group-text">-</span>
                        <input asp-for="MaxPrice" class="form-control" placeholder="Đến" />
                    </div>
                </div>

                <div class="col-md-3">
                    <label class="form-label">Tên mặt hàng</label>
                    <div class="input-group">
                        <input asp-for="SearchValue"
                               class="form-control"
                               placeholder="Tên mặt hàng..." />
                        <button type="submit" class="btn btn-info">
                            <i class="bi bi-search"></i>
                        </button>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>

<div id="searchResult"></div>

@section Scripts {
    <script>
        document.addEventListener("DOMContentLoaded", function () {
            // Thực hiện tìm kiếm ngay khi trang web vừa tải xong
            paginationSearch(null, document.getElementById("formSearch"), @Model.Page);
        });
    </script>
}